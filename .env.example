# ── LLM Provider ─────────────────────────────────────────────────────────────
# "github"  = use your GitHub Copilot license (GitHub Models endpoint)
# "openai"  = use a direct OpenAI API key
LLM_PROVIDER=github

# ── GitHub Copilot / GitHub Models ───────────────────────────────────────────
# Generate a PAT at https://github.com/settings/tokens
# Required scope: (no extra scopes needed if you have Copilot)
GITHUB_TOKEN=ghp_...

# ── OpenAI (only needed if LLM_PROVIDER=openai) ─────────────────────────────
# OPENAI_API_KEY=sk-...

# ── Model settings ───────────────────────────────────────────────────────────
# Models available via GitHub Models: gpt-4o, gpt-4o-mini, etc.
REVIEWER_MODEL=gpt-4o

# Max tokens for the review response
MAX_TOKENS=4096

# Severity threshold: info, warning, error (only show issues >= this level)
SEVERITY_THRESHOLD=info
